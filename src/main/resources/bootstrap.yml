spring:
  profiles:
    active: dev

---

spring:
  profiles: dev
  application:
    name: attendance-service
  cloud:
    config:
      uri:
      username:
      password:



---

spring:
  profiles: prod
  application:
    name: attendance-service
  cloud:
    config:
      uri:
      username:
      password: